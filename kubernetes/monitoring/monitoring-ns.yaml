apiVersion: v1  # Specify the Kubernetes API version for the Namespace
kind: Namespace  # Specify the resource kind as Namespace
metadata:
  name: monitoring  # Specify the name of the Namespace
  labels:
    app: karsajobs  # Labels for the Namespace
